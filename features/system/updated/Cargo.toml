[package]
name = "devcore-updated"
description = "DevCore read-only update and release D-Bus boundary"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
devcore-execution = { path = "../../../shared/execution" }
devcore-update = { path = "../update" }
zbus = { version = "=4.4.0", default-features = true, features = ["option-as-array"] }

[lints]
workspace = true
