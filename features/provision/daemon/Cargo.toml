[package]
name = "devcore-provisiond"
description = "On-demand user-session post-install developer package service"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
devcore-core = { path = "../../../shared/core" }
devcore-execution = { path = "../../../shared/execution" }
devcore-provision = { path = "../domain" }
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.145"
zbus = { version = "=4.4.0", default-features = true, features = ["option-as-array"] }

[lints]
workspace = true
