[Unit]
# The live session owns VT1 directly.  Fedora's stock greetd ordering waits
# for getty@tty1, which is intentionally masked on this ephemeral image.
After=
After=systemd-user-sessions.service plymouth-quit-wait.service
Conflicts=

[Service]
StandardOutput=journal+console
StandardError=journal+console
