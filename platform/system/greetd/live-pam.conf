# The live image is an ephemeral, auto-login session.  Keep this PAM stack
# local to the installer media; the embedded BaseOS payload retains Fedora's
# normal authentication policy for the installed system.
auth      requisite pam_permit.so
account   requisite pam_permit.so
session   required  pam_systemd.so
session   required  pam_permit.so
